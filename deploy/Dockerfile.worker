FROM node:24.21.0-bookworm-slim
# poppler-utils: pdfinfo + pdftoppm split multi-page PDFs into page images (services/ocr-worker/src/split.ts).
# prlimit (util-linux, essential in Debian) caps each renderer process's address space and CPU time.
RUN apt-get update \
 && apt-get install -y --no-install-recommends poppler-utils \
 && rm -rf /var/lib/apt/lists/* \
 && pdftoppm -v \
 && command -v prlimit
WORKDIR /app
COPY package.json pnpm-workspace.yaml pnpm-lock.yaml tsconfig.base.json tsconfig.json ./
COPY apps ./apps
COPY packages ./packages
COPY services ./services
COPY prisma ./prisma
RUN corepack enable && pnpm install --frozen-lockfile
CMD ["pnpm", "--filter", "@innovera/ocr-worker", "start"]
